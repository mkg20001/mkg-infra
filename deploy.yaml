groups:
  some-group:
    - some-host

config:
  dataDirectory: '/etc/enter-script-state-location-here'

modules:
  auth:
    username:
      keys: ['gh:githubName', 'lp:launchpadName']
      groups: ['sudo']
  backup:
    files:
      - /path/to/file
    folders:
      - /path/to/folder
    cmds:
      - bla...
    cmdOut: # files generated by cmds that should be backup'd
      - /path/to/cmdOutput
    rmCmdOut: true # del files after backup
