affects:
  - '*'
priority: 20
modules:
  pkg:
  # htop: process monitor
    - htop
  # nload/nethogs: network monitor
    - nload
    - nethogs
  # iotop/nmon: disk monitor
    - iotop
    - nmon
  # wget/curl: HTTP
    - wget
    - curl
  # nano: editor
    - nano
  # tree/ncdu: file analytics
    - tree
    - ncdu
  # glances: netdata for cli, basically
    - glances
